const txStatus = {
  pending: 'Pending',
  notExecuted: 'Not Executed',
  invalid: 'Invalid',
  success: 'Success',
  failed: 'Failed', // TODO: remove when ready
  fail: 'fail',
};

export default txStatus;
